import React from 'react';
import { ViewProps } from 'react-native-ui-lib';
import { PoolNavigator } from './pool-navigator';

export const PoolView: React.FC<ViewProps> = () => <PoolNavigator />;

export default PoolView;
